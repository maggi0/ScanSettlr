<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Name="AddExpenseGroup"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:controls="clr-namespace:ScanSettlr.Controls"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ScanSettlr.AddExpenseGroupPage"
             BackgroundColor="#218CA1">

    <Grid RowDefinitions="Auto,*">
        <!-- Top bar -->
        <controls:TopBar Title="New Expense Group"
                         ShowSettingsButton="False"/>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="40">

                <Label Text="Name:"
                       Padding="5"
                       TextColor="White"/>
                <Border Stroke="Black">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="5"></RoundRectangle>
                    </Border.StrokeShape>
                    <Entry x:Name="nameEntry"
                           Placeholder="Name"
                           BackgroundColor="White"/>
                </Border>

                <Button Text="Add new group"
                        Margin="0,50,0,0"
                        BorderWidth="1"
                        BorderColor="Black"
                        TextColor="Black"
                        BackgroundColor="#BDF8BF"
                        HeightRequest="50"
                        CornerRadius="10"
                        Command="{Binding CreateGroupCommand}"/>
                
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
